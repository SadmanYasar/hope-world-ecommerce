apiVersion: v1
kind: Service
metadata:
  name: spring-boot-hopeworld-service
spec:
  type: LoadBalancer
  selector:
    app: spring-boot-hopeworld 
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080